import styled from "styled-components";

const Main = styled.main`
  padding: 6rem 0;
  display: flex;
  gap: 2rem;
  align-items: flex-start;
  width: 90%;
  margin-inline: auto;
`;

export { Main };
